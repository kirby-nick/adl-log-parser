<h5>Edit Comment</h5>

<%= @comment.body %>

<%= form_with(model: @comment, url: event_log_comment_path) do |f| %>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<br>
<%= link_to "Event Log", event_log_path(@comment.event_log) %>
